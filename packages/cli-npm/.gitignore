# Binaries ARE included in NPM package
# (See package.json "files": ["bin", "lib", "package.json"])
# bin/*.exe
# bin/shadow-secret
# bin/shadow-secret-*

# Node modules (development)
node_modules/

# NPM tarballs (development)
*.tgz
