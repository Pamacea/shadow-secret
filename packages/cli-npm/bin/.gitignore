# Ignore all compiled binaries
# These are injected during CI/CD before publishing to NPM

# Windows executable
*.exe

# Unix binary
shadow-secret

# macOS binary (if different naming)
shadow-secret-macos
shadow-secret-darwin

# Linux binary (if different naming)
shadow-secret-linux
